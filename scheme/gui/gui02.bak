;; gui02.scm
;; Geoffrey Matthews
;; 2005

;; Building gui's with MrEd

(define frame (new frame% 
                   (label "Example")
                   (min-width 100)
                   (min-height 80)
                   ))
(send frame show #t)
